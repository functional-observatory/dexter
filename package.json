{
  "name": "dexter",
  "private": true,
  "version": "0.1.0",
  "main": "index.js",
  "license": "MIT",
  "devDependencies": {
    "engine-phi": "../phi/",
    "netlify-cli": "^2.59.2"
  },
  "scripts": {
    "dev": "phi dev",
    "build": "phi build",
    "deploy": "netlify deploy --dir=build --prod",
    "deploy:dry": "netlify deploy --dir=build"
  }
}
